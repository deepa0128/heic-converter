# URL to POST the converted JPEG to as multipart/form-data.
# If not set, the Lambda returns the JPEG as base64 in the response body.
WEBHOOK_URL=

# Default JPEG quality (0.0–1.0). Overridden per-request by the event's `quality` field.
# Defaults to 0.92 if not set.
JPEG_QUALITY=0.92
